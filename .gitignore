vowelcharts/
.sconsign.dblite
*.lib
*.exp
*.pdb
*.dll
*.pyc
*.pyo
build/
build-x64/
build-x64-nvda/
build-x86/
.claude/
briefing.md
claude-release.md
rln-*.txt
nvdaAddon/synthDrivers/tgSpeechBox/speechPlayer.py
tools/gen_nvda_api_ref.py

# ── Build artifacts and release staging ──────────────────────
build-test/
release/
installer/Output/
nul

# ── Test artifacts ───────────────────────────────────────────
# Rendered audio + phoneme-trace sidecars from tgsb_unit_tests audit
# tests, formant-comparison drivers, etc. Regenerable, large, binary —
# never commit (the dir gets caught accidentally by `git add .`).
tests/wav_samples/

# ── Mobile (platforms/) ──────────────────────────────────────
# Android
src/platforms/android/local.properties
src/platforms/android/app/build/
src/platforms/android/app/.cxx/
src/platforms/android/.gradle/
src/platforms/android/build/
signing.properties
*.keystore
*.jks
*.p12
*-service-account*.json
src/platforms/android/upload_play.py

# iOS
src/platforms/ios/*.xcodeproj/xcuserdata/
src/platforms/ios/*.xcodeproj/project.xcworkspace/xcuserdata/
src/platforms/ios/build/
*.mobileprovision
ExportOptions.plist
AuthKey_*.p8

# Shared mobile
*.raw
# Local Codex project memory
/.codex-memory/
/AGENTS.md
# Tool caches (HuggingFace hub etc.)
/.cache/
# Root artifact zoo (release binaries live on GitHub releases; renders,
# traces and listening zips are transient) -- archived to C:/git/tgsb-archive
/test_output_*
/*.wav
/*.mp3
/*.apk
/*.aab
/*.exe
/*.nvda-addon
/*.zip
/testflight-notes.txt
